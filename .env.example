# Base path for GitHub Pages (e.g. /fullstack-mastermind/). Leave empty for root deploy.
# VITE_BASE_PATH=/

# Optional: override content folder name (default: content/ inside app, else ../60-day-sprint)
# VITE_CONTENT_DIR=content
